/**
 * Basic parameters for the music produced.
 * 
 * @author Matthew Else <matthew.else@cl.cam.ac.uk>
 */

export const bpm = 240;
export const duration = 4; // minute
export const minutes = duration;
export const bars = bpm * (minutes / 4);
